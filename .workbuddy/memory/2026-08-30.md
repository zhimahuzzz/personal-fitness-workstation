# 2026-08-30

## 健身工作台项目启动
- 与用户多轮问答确定需求：手机优先响应式网页 + Supabase 免费云端（邮箱密码登录）+ 规则计算预留AI + 增肌塑形目标
- 关键纠正：饮食照片只做 AI 识别热量的临时中转（算完即删）；进度照片每~10天一张长期保存
- 开发节奏：逐模块确认
- M1 已完成编码：Vite+React 骨架、底部五标签导航（仪表盘/训练/饮食/状态/我的）、登录注册页（Supabase Auth，未配置时降级提示）、SETUP-SUPABASE.md 配置指南
- 待办：等 npm install 完成后启动 dev server 给用户预览 M1；用户需按指南注册 Supabase

## 补充（晚间）
- dev server 已跑在 localhost:5173，M1 已交付预览
- 应用正式定名：老张健身工作台；称呼用户：老板（已改 index.html 标题与登录页）
- 用户问 Supabase 建项目时的 "Enable automatic RLS"：已答复建议勾选，并在 SETUP-SUPABASE.md 中补充说明（勾选后建表 SQL 需带 RLS 策略，后续每张表都会提供）
- 用户提供 Publishable key（新版 anon key 格式 sb_publishable_）和项目 URL，已写入 .env.local；用 /auth/v1/health 验证连通成功；dev server 已重启加载环境变量
- 注意：用户建项目时勾选了 automatic RLS，后续所有建表 SQL 必须附带 USING (auth.uid() = user_id) 类型的策略
- 待用户在登录页注册账号验证 M1，之后开工 M2 训练模块

## M2 训练模块（深夜开始）
- M1 验收通过：用户注册账号成功，云端登录打通
- M2 编码完成：supabase/m2-training.sql（exercises/workouts/workout_sets 三表 + RLS 策略 + 40个预置动作，幂等可重复执行）+ Training.jsx（训练列表/进行中会话/详情三视图）
- 功能：动作选择器（搜索+肌群筛选+自定义动作）、组数×次数×重量记录、自动显示上次成绩与历史最好（估算1RM=weight×(1+reps/30)）、新组自动预填上次数据、训练草稿存 localStorage 防刷新丢失、本周训练次数/容量统计
- 待办：用户需在 Supabase SQL Editor 手动执行 m2-training.sql 建表，然后测试记录一次训练
